describe('DynamoDbLib', function () {
  describe('call', function () {
    it('Should perform the call using DynamoDB', async function () {
      // DynamoDB['put'] = () => { };
      // const dynamoDBSpy = spyOn(DynamoDB, 'put' as any);
      // const expectedOperationResult = {
      //   expected: 'operationResult',
      // };
      // dynamoDBSpy['put'].and.callFake(() => expectedOperationResult);
      //
      // await DynamoDbLib.call('put', { param: 'paramValue' })
      //
      // expect(dynamoDBSpy['put']).toHaveBeenCalledTimes(1);
    });
  });
});
